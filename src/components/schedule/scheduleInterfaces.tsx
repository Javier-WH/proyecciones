export interface ScheduleItem {
  id?: string;
  hours_id: string;
  teacher_id: string;
  day_id: string;
  subject_id: string;
  classroom_id: string;
  seccion: string;
  trayecto_id: string;
  turn_id: string;
  pnf_id: string;
  quarter: string;
}

